{
  "name": "roots-livepoll",
  "version": "1.1.0",
  "private": true,
  "description": "ROOTS Live Poll – Interaktive Live-Polling- und Präsentations-Plattform",
  "scripts": {
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "format": "prettier --write \"**/*.{js,css,html,json,md}\"",
    "format:check": "prettier --check \"**/*.{js,css,html,json,md}\"",
    "check": "tsc --allowJs --noEmit --checkJs --target es2020 --module esnext --moduleResolution node --strict false app.js viz.js templates.js lp-core.js lp-features.js",
    "test:smoke": "playwright test"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0",
    "@playwright/test": "^1.40.0"
  }
}
